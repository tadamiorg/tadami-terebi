<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.TadamiTerebi" parent="@android:style/Theme.Material.NoActionBar">
        <!-- Doubles as the cold-start splash: centered logo on black until Compose mounts. -->
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:colorBackground">#202125</item>
    </style>
</resources>
